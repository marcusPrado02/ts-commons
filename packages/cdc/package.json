{
  "name": "@acme/cdc",
  "version": "0.1.0",
  "description": "Change Data Capture: event normalisation, filtering and transformation",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint src --ext .ts --max-warnings 0"
  },
  "devDependencies": {
    "vitest": "2.1.9",
    "eslint": "^8.57.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
