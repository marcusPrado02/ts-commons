{
  "name": "@marcusprado02/hotreload",
  "version": "0.1.0",
  "description": "Hot reload utilities — watch mode, incremental compilation and fast test refresh for @marcusprado02/ts-commons",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts --max-warnings 0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "vitest": "^2.1.9"
  },
  "keywords": [
    "hot-reload",
    "watch",
    "incremental",
    "compilation",
    "dx",
    "developer-experience"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  }
}
