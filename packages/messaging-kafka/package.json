{
  "name": "@acme/messaging-kafka",
  "version": "0.1.0",
  "description": "Apache Kafka adapter for event-driven messaging with consumer groups and exactly-once semantics",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "kafka",
    "messaging",
    "event-driven",
    "consumer-group",
    "clean-architecture"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@acme/kernel": "workspace:*",
    "@acme/messaging": "workspace:*",
    "@acme/observability": "workspace:*",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "vitest": "^2.1.9"
  }
}
