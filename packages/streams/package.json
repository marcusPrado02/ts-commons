{
  "name": "@marcusprado02/streams",
  "version": "0.2.0",
  "description": "Stream processing: observable streams, operators, backpressure, merging/splitting and windowing for @marcusprado02/ts-commons",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts --max-warnings 0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^2.1.9"
  },
  "keywords": [
    "streams",
    "observable",
    "backpressure",
    "windowing",
    "operators"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  }
}
