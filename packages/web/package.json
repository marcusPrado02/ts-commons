{
  "name": "@acme/web",
  "version": "0.1.0",
  "description": "Web adapters: HTTP server abstractions and middlewares",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acme/kernel": "workspace:*",
    "@acme/errors": "workspace:*",
    "@acme/contracts": "workspace:*"
  },
  "keywords": ["web", "http", "fastify", "middleware"],
  "author": "",
  "license": "MIT"
}
