{
  "name": "@acme/web",
  "version": "0.1.0",
  "description": "Web adapters: HTTP server abstractions and middlewares",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@acme/kernel": "workspace:*",
    "@acme/errors": "workspace:*",
    "@acme/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^3.2.4"
  },
  "keywords": ["web", "http", "fastify", "middleware", "hateoas", "hal", "json-api"],
  "author": "",
  "license": "MIT"
}
