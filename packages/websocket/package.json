{
  "name": "@marcusprado02/websocket",
  "version": "0.1.0",
  "description": "WebSocket server, rooms, broadcast, authentication and reconnection",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint src --ext .ts --max-warnings 0"
  },
  "devDependencies": {
    "vitest": "4.1.2",
    "eslint": "^8.57.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  }
}
